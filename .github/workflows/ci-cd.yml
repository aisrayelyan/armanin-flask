name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      DEPLOYMENT_FILE: "k8s/deployment.yaml"
      SERVICE_FILE: "k8s/service.yaml"
      INGRESS_FILE: "k8s/ingress.yaml"
      POD_FILE: "k8s/pod.yaml"
      POSTGRESQL_SERVICE_FILE: "k8s/postgresql-service.yaml"
      POSTGRESQL_STATEFULSET_FILE: "k8s/postgresql-statefulset.yaml"

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Install and setup ngrok for SSH
      uses: tmshkr/ngrok-ssh@latest
      with:
        NGROK_AUTHTOKEN: ${{ secrets.NGROK_AUTHTOKEN }}
        NGROK_CONFIG_FILE: "ngrok.yml"
        WAIT_FOR_CONNECTION: true

    - name: Install SSH key
      uses: webfactory/ssh-agent@v0.5.3
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v3
      with:
        context: .
        push: true
        tags: ${{ secrets.IMAGE_NAME }}

    - name: Deploy to Kubernetes
      uses: appleboy/ssh-action@v0.1.7
      with:
        host: ${{ secrets.K8S_HOST }}
        username: ${{ secrets.K8S_USER }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        port: ${{ secrets.K8S_PORT }}
        script: |
          kubectl apply -f ${{ env.DEPLOYMENT_FILE }}
          kubectl apply -f ${{ env.SERVICE_FILE }}
          kubectl apply -f ${{ env.INGRESS_FILE }}
          kubectl apply -f ${{ env.POD_FILE }}
          kubectl apply -f ${{ env.POSTGRESQL_SERVICE_FILE }}
          kubectl apply -f ${{ env.POSTGRESQL_STATEFULSET_FILE }}

    - name: Start application
      run: npm start

    - name: Long-running process
      run: sleep 3600  # Оставляем workflow активным на час
